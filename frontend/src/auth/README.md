# Auth

OIDC/JWT wiring against the auth seam (AWS Cognito in production, a dev
issuer locally) via `react-oidc-context` lands here in a later phase — this
directory is reserved by the plan-pinned layout.
