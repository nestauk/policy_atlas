# Decision sheet — spec changes proposed by feasibility checks 6, 2, 3, 4, 5

Every proposal below is the owner's to accept, reject or edit. Nothing here is in the specs yet
(an earlier unilateral amendment pass was reverted, 2026-09-08). When a row is decided, the spec
is edited to the ruling, quoting the decision. Rows are grouped by the spec they would change.
"Rec." is the agent's recommendation only.

Legend for *Decision*: accept · reject · edit (say how) · defer.

## When to decide each row (owner, 2026-09-08: decide in the task the row concerns)

| decide at | rows | why then |
|---|---|---|
| **before task 1's contract** (owner decision 2026-09-08: the fifteen, taking the pass-4 additions) | A6, A7, A8, F1 (the inherit records, declared in both capability specs); C1, C2 (evidence-scope face and code name); C3, D3, E13 (user context and what task 1 must retain); C4 (multi-scope plans); A4 in reduced form (does task 1's `inherit` reuse inherited findings, or copy them?); A5 (honest parse and reading-depth labels, which baseline claims rely on); B3, C5, E12, E16 (the baseline's budget and honest timing language) | these define the shared records task 1 creates and later tasks build on, and what task 1 may promise |
| task 2 (longlist) | A1, A2, A9, A10, A11, E1, E2, E4, E5, E6, F2, F3 | the option entity, the mention profile, the shortlist quota guards |
| task 3 (shortlist and assessment) | A3, B1, B2, D1, D2, D4–D6, E3, E7–E11, F4, F6 (B3, C5, D3, E12, E13 moved before task 1) | the light profile, reading scope and eligibility, the transferability block, "how sure", the cap setting |
| task 5 (full run) | E14, E15, F5 (F1 moved before task 1) | the derivation edge, membership against a specified design in the child |
| any time, as small Evidence search slices | A4 (full form); A5's implementation (its requirement is decided before task 1) | the memo key and the `text_basis` label; each wants an ADR |
| at post-build user testing (check 1, deferred by the owner 2026-09-08) | revisit anything the testing contradicts: the shortlist ceremony, the comparison principle, the handoff | check 1 is the one check that can change the shape; live asks are not feasible before the build |


## A — data-model.md

| # | proposal | where | from | rec. | ChatGPT suggestion | decision |
|---|---|---|---|---|---|---|
| A1 | The **option is an addressable-unit kind**, so claims about an option (ambition tag, coverage patterns, constraint judgements, reasoned guesses, relation rationales) are ordinary annotations keyed `(option, type)`. Today annotations key on `(block, unit)` and unit ids change on regeneration; the option id does not. | § Atomic units; § Option entity | 6 F1 | accept | edit — unchanged after extra runs; [Check 6 F1](../check-6-contract-trace.md) identifies a missing claim anchor by tracing contracts, not by testing regeneration. Use “Claims attach to the option id and the design version, with the evidence and judgement version recorded; a changed design cannot inherit a current verification merely because its option id survives.” Task 2 needs this to preserve rulings 36 and 44 without weakening the system's version-bound annotations. | |
| A2 | **`intervention_mention` as a third finding kind** (the abstract profile): the shared reference vocabulary at mention grain plus `design_features`, `role` (evaluated · described · recommended · comparator · mentioned), `is_bundle`/`components`, an **adoptability** flag, a quote anchor; document-level `design_hint`, `names_no_intervention`. Joins `finding_reference_union`. Own fingerprint domain. | § Findings layer | 6 F2; 3 C3-3 | accept | edit — remains edit after extra runs: [runs 1–3](extra-runs.md) produce four separately named no-document entrants, but NEET still leaves 28 of 124 units unassigned and the gpt-5.4-mini model judge calls two mention-path outputs non-options. Use “Store source mentions and roles separately from a reviewable judgement about actionable options; preserve suggestions and unresolved candidates rather than deleting them on an adoptability flag.” Task 2 still needs rulings 31, 36 and 43, and these observations do not validate that flag or prove recall. | |
| A3 | **Light profile = IOF subset + four fields** (`design_features`, `magnitude_as_reported` as number-and-unit only, `period`, `trial_or_registration_id`) writing `intervention_outcome_finding` under its own profile id, plus a document-level **`study_identity`** (registration id; programme or trial name matched name-first; protocol-only; reports-own-data). | § Findings layer | 2 C2-2, C2-1 | accept | edit — remains edit after extra runs: [run 5](extra-runs.md) groups four JU:MP papers together, but `trace2_v2.json` still uses document ids as study keys when identity is absent, and two studies sharing a programme name remain untested. Use “Keep the coherent effect claim with comparator, population, period, uncertainty and observed/modelled basis; count independent studies only after supported identity and overlap checks, never from a fallback document id.” Task 3 needs this under rulings 26, 33 and 35; the model judge's agreement on supplied examples does not establish the missing independence rule. | |
| A4 | **Memo key for acquired snapshots drops `task_id`**; requirement resolution at **field grain within a schema** with the intervention-as-implemented as a match key. As built the memo is task-scoped and whole-record; rulings 35 and 48 depend on this in both directions. *Evidence search change → ADR.* | § Findings layer intro | 6 F3; 2 C2-2 | accept, own ADR | edit — remains edit after extra runs: [run 5](extra-runs.md) gives a model-judge assessment of one simulated older record's missing fields, but performs no cross-task reuse or field-completion operation. Use “Reuse only compatible parts of a finding for an authorised linked snapshot, preserve the original claim and provenance, and fill or label missing requirements without mixing comparators or periods; decide wider memo sharing separately.” Before task 1 choose this bounded inheritance behaviour under rulings 35 and 48, without treating the model judge's compatibility opinion as evidence for removing `task_id`. | |
| A5 | **`text_basis` derived from the parse result**, not the fetch (a minimum-size or parse-success rule, or a third value `full_text_failed_parse`). Five of 21 read-set "full text" documents were failed parses of under 800 characters. *Evidence search change → ADR.* | § Corpus & snapshots | 2 C2-7 | accept, own ADR | edit — remains edit after extra runs: pinned `light_v2.json` still records abstract fallback for the 284-character `e12285a6` parse, while [run 7](extra-runs.md) logs two parse failures and 25 fetch failures in the fresh search. Use “Record obtained, parsed and actually inspected text separately; derive claim depth from what was inspected, leaving failed or partial reading explicit.” Task 1's baseline already needs ruling 16, and neither these measurements nor the runner's size threshold validate complete-full-text status. | |
| A6 | **`task_link`** record (source task · target task · kind · optional option id · created by/at): the input `inherit` reads in both directions; the option entity's child-task link is one row of it. | new § Links between tasks | 6 F6 | accept | accept — unchanged after extra runs; [Check 6 F6](../check-6-contract-trace.md) finds that the declared Link input has no record; this is a contract/schema observation, not a runtime test. A durable link with source, target and provenance is needed before task 1 and can later carry task 5's option link under rulings 22, 44 and 48; the record itself grants no access to the source task. | **ACCEPTED 2026-09-09** — generic task-to-task link, option id kept; same-project only; inherit reads across (no copies) with pinned source run ids; archive-not-delete; a link changes no access. Applied to data-model.md § Links between tasks. |
| A7 | **`inherited_from_task_id`** on `task_source_snapshot`, not a new `origin` value. Classify and appraise rows are **copied as inherited assertions** when the classifier or rubric version matches, else re-run (closes the 🟡 in OS components § 0). | same | 6 F7 | accept | edit — unchanged after extra runs; [Check 6 F7](../check-6-contract-trace.md) observes the task guard and closed origin values, but copy-on-matching-version is an untested reuse policy. Use “Copy an inherited classification or appraisal only for the same snapshot and compatible inputs, classifier/profile and rubric versions, preserving the source assertion; otherwise rerun and retain the prior assertion in history.” Task 1 should retain the separate inherited marker and mandatory re-screening under rulings 22 and 48; a matching rubric alone does not establish compatibility. | **ACCEPTED as revised 2026-09-09** — no inheritance column: inherited = created by the inherit run, source resolved via task_link by shared snapshot (handles many sources); classify/appraise read from the pinned run; versions not surfaced to users; re-run = plan setting or user action; re-screen mandatory. Applied to data-model.md § Corpus and OS components § 0. |
| A8 | **Document identity for counting** = DOI or normalised title plus year, resolved at `inherit` and `longlist`; one review present as two snapshots counts once. | § Option entity | 2 C2-5 | accept | edit — remains edit after extra runs: [run 5](extra-runs.md) and `trace2_v2.json` now expose five copies of one review, split across three name keys, which also inflate run 6's contrary counts. Use “Resolve confirmed document copies before selection and counting, retaining their snapshots and citations; use DOI and normalised bibliographic matches as evidence of identity, with ambiguous matches unresolved.” Task 1's inheritance, task 2's coverage and task 3's cap all need this under rulings 26, 31 and 33; the model judge confirms the examples, not a generally safe title/year merge rule. | |
| A9 | Relations gain **instance of** (or *part of* reused with a type): mentions cluster to class-grain options, deep findings to named implementations; a class option carries its named implementations beneath it. | § Option entity | 3 C3-1 | accept | edit — moves from defer after [run 2](extra-runs.md): within the same 22-document selection, 84 of 89 finding units and all six review-derived units fit mention-path options, supporting a bounded relationship between broader classes and specified designs. Use “Where both are useful, show named implementations beneath a broader class with an explicit relationship; assess only the specified design and reserve part of for packages and ingredients.” Task 2 can declare this limited behaviour under rulings 15, 31 and 36, while the gpt-5.4-mini model judge's grain labels, unequal available findings and untested usefulness do not justify a compulsory two-level longlist. | |
| A10 | Lever types stored as **primary, secondary and runner-up** with the one-sentence reason (a runner-up was named for 18 of 29 and 27 of 32 options). | § Option entity | 3 C3-4 | accept | edit — remains edit after extra runs: [run 1](extra-runs.md) measures changed NEET selections under three of four lever perturbations, although only two of 20 options have a runner-up label in `neet.stability.json`. Use “Keep primary and secondary mechanisms distinct from uncertainty about the primary; retain alternative judgements and their effect on selection in the run record.” Task 2 should not assume a runner-up flag detects every unstable place or add a third required user classification under ruling 20. | |
| A11 | Membership records carry an **assignment stability marker** (class options lost a third of members under paraphrase; named designs kept theirs) and admit **design feature not stated** as a third outcome, attaching to the parent class only (ruling 36 made concrete). | § Option entity | 3 C3-5; 2 C2-3 | accept | edit — remains edit after extra runs: [runs 2 and 3](extra-runs.md) show stable finding assignments in one paraphrase test and four surviving suggestions, but pinned `trace2_v2.json` still attaches the unstated-sanction mention to the sanction-free variant. Use “Record assignment reasons and unresolved features against the specified design/version, withhold unsupported design matches, and preserve uncertain members in coverage rather than counting only the stable core.” Tasks 2 and 3 still need rulings 36 and 39; survival of an injected option is not measured correctness of its support or persistence through edits. | |

## B — execution-orchestration.md

| # | proposal | where | from | rec. | ChatGPT suggestion | decision |
|---|---|---|---|---|---|---|
| B1 | Resolve the reading-budget seam: **`reading_scope`** parameter on `synthesise` — snapshots readable at full-text depth (the read set), at abstract depth (the option's mentioning documents), plus declared exceptions; chunks outside are not retrievable in that composition; omissions represented. Distinct from the data model's soft-prior rule for intent scoping. | § Vocabulary (the seam note) | 6 F4 | accept | edit — remains edit after extra runs: [run 6](extra-runs.md) now compares the same text window per document, but tests no enforced reading boundary across synthesis tools, inherited findings or parent-evidence exceptions. Use “Declare and enforce permitted sources and depths for every read path and section, record omissions, and count any exception's reading against an explicit budget.” Task 3 still needs this under rulings 15, 16, 38 and 44; equal input in a test is not runtime enforcement. | |
| B2 | **Eligibility is a record**: the option's finding-grain membership, written by `longlist` in assign-only mode after extraction; ⟨assess⟩ composes that step between extract and synthesise. | same | 6 F4 | accept | accept — remains accept after extra runs: [run 6](extra-runs.md) assigns only six of 49 quote-matched T3 findings to the design, strengthening the need for finding-level eligibility before synthesis. Task 3 should retain versioned assignment records under rulings 36, 39 and 44, while the six is a count of model assignments, not an independently established count of relevant evidence; the different T3 assignments in `trace2_v2.json` and `transfer_jump.json` still need reconciliation. | |
| B3 | Record the measurement: **the rapid budget is a synthesis budget** — synthesise is 72 percent of a standard walk's compute on staging, extraction 17 percent of a deep walk; the latency lever is the terminus's section count, turn caps and reading scope. | same or plan-as-object § depth | 5 C5-1 | accept | edit — remains edit after extra runs: [the pinned fan-outs and run 7](extra-runs.md) now measure 24.6/16.0 seconds for extraction and 380.224 seconds for the proxy synthesis, removing the missing-timing objection for those new runs. Use “These runs make synthesis the first place to investigate; measure the actual baseline, profile, verification and total path before setting a scoping budget.” Task 1 and task 3 need ruling 38, because the 675.7-second Evidence search reused 78 of 100 acquired sources and does not prove cold OS latency or that document caps never affect it. | |

## C — plan-as-object.md

| # | proposal | where | from | rec. | ChatGPT suggestion | decision |
|---|---|---|---|---|---|---|
| C1 | The **evidence-scope constraint as the policy's third face**, compiling to `scope_filters` where a backend can express it and to a **deterministic set-aside** over abstract-profile columns after extract(abstract), before `longlist` — a status per document × constraint, counted on Sources, never support, never an option exclusion. It cannot act at the screen (relevance-only; runs before study geography is known). | § Source / evidence policy | 6 F5 | accept | edit — remains edit after extra runs: [run 3](extra-runs.md) sets aside five documents but exercises no option whose only source is set aside, and the runner uses a country-name substring list rather than a complete scope rule. Use “Keep evidence scope distinct from source quality; preserve known candidates, retain set-aside reasons, and leave missing or mixed geography unresolved rather than guessing a document-wide answer.” Task 1 must settle baseline use before task 2's enforcement under rulings 23, 36, 43 and 44; the measured five exclusions do not validate the policy. | |
| C2 | Name the **code-name collision**: the as-built `evidence_scope` table is the intent record; the constraint carries a different code name. | same | 6 F5 | accept | accept — unchanged after extra runs; [Check 6 F5](../check-6-contract-trace.md) observes that `evidence_scope` already names the intent record, so using it for a constraint would conflate two meanings. Choose a distinct internal field name before task 1; this is a contract naming correction, not measured product behaviour, and preserves rulings 23 and 44 without introducing new words in the product. | |
| C3 | **Typed user-context entries** under Assumptions & boundaries — *stated by you* / *planned by you*, verbatim `user_text`, date, decision-event provenance; versioned with the plan; carried by `inherit`; the entry's type is what decides whether it can lift a cap. | § What a plan contains | 6 F10 | accept | edit — remains edit, with a stronger provenance requirement after [run 4](extra-runs.md): commitments stay planned, but the promotion code labels a user's account of an audit retrieved without a fetched source or verified citation. Use “Preserve the user's words and stated/planned type, source claim, target and dates; mark context retrieved only when the actual source is obtained and cited, and do not infer missing dates or applicability.” Task 1 must retain that distinction for tasks 3 and 5 under rulings 18, 34 and 41; the reported six successful classifications are not a model-judge validation of sourced context. | |
| C4 | A plan may compile to **more than one intent record** (baseline scope, longlist scope, one per variant, one per thin option's targeted acquire); coverage unions across scopes; membership keys on the task's document row. | same | 6 F12 | accept | accept — unchanged after extra runs; [Check 6 F12](../check-6-contract-trace.md) shows on paper why baseline, longlist and targeted searches require distinct intent records; it does not test their lifecycle. Task 1 should declare the multiple-scope relationship and task-document identity, with coverage unioned without double counting and each scope tied to its approved plan version, supporting rulings 15, 22, 36 and 44. | |
| C5 | Record the measured depth numbers (standard walk median 15 min, p90 29; gate waits median 17 min, p90 2 h) and set the **per-option document cap as a plan setting with defaults 5 (rapid) and 8 (standard)**; state time-to-result as compute time and, separately, elapsed time including gates. | § Thoroughness | 5 C5-4, C5-5 | edit — revised after pass 4: caps 5/8 as trial settings; 15 of 36 documents lacked wall times | edit — remains edit after extra runs: [run 6](extra-runs.md) loses T3's one identified contrary finding at cap 3 and retains it at cap 5, but T4 still loses three contrary records at cap 8 before duplicate review records are reconciled. Use “Expose the cap and incomplete coverage in the plan; keep 5 and 8 as trial settings, and separate compute from elapsed time without defaulting depth.” Task 1 needs honest timing language and task 3 needs a corrected cap choice under rulings 25 and 38; these model-dependent counts do not establish universal defaults. | |

## D — provenance-grounding.md (column-grounded blocks)

| # | proposal | where | from | rec. | ChatGPT suggestion | decision |
|---|---|---|---|---|---|---|
| D1 | The block is produced in **two steps**: factor extraction from the evidence once (the two evidence legs judged from evidence and the stated target; one row per support factor with ids, quote, evidence basis), then **context fill** against that fixed list. A single prompt drifted its factor set and tied the evidence legs to context. | § Column-grounded blocks | 4 C4-1 | edit — revised after pass 4: the factor list must be pinned to a version; repeat extraction shared 4 of 12 factors | edit — remains edit, with a required check of factor content after [run 4](extra-runs.md): the three runs produce 12/13/13 rows and 0/3/0 dealbreakers, while the reported one shared factor counts exact labels rather than equivalent meanings. Use “Keep a recorded factor list for each evidence/design/target version, check its support and missing necessary conditions before deriving a verdict, and recheck it when those inputs change.” Task 3 needs this under rulings 18, 34 and 43; pinning an unchecked list preserves mistakes, and clustering the context claims is a proposed remedy rather than a tested one. | |
| D2 | **Verify runs code-side rules** and records corrections as flags: planned → never met; retrieved at containing geography → met/not met only when *applies by nature*; dated → unknown; entry id must exist. Five of five model over-statements in the test were caught this way. | same | 4 C4-5 | edit — revised after pass 4: two over-statements and three evidence-leg restorations, one restoring a wrong status; enforcement bounds context, not evidence | edit — remains edit after [run 4](extra-runs.md): the gpt-5.5 model judge disagrees on four of 20 selected rows, including unsupported coordination, deliverer and buy-in claims, while the raw file records two evidence-status restorations rather than the reported planned-entry correction. Use “Enforce typed limits in code, verify that each entry establishes the particular factor, and check whether any exception actually applies to the target; leave unresolved support Unknown.” Task 3 still needs rulings 18, 34 and 43, and neither a model-assigned factor tag nor an untested exception field guarantees that verification. | |
| D3 | The *retrieved* context cell carries **geography level and observation date**. | same | 6 F11; 4 C4-6 | accept | edit — remains edit after [run 4](extra-runs.md): the audit year survives chat promotion, but missing dates are filled with 2026 by the runner and the cited audit has not been retrieved. Use “Retain observation period, geographic coverage and the actual source separately from when the user spoke; leave an unstated date unknown and explain currency before using the fact.” Task 1 must preserve this for task 3 under ruling 34; the model's date extraction is an observation, not validation of a currency rule. | |
| D4 | The **causal-role leg defaults to Unknown**: *not met* requires a stated contradiction between evidence population or deliverer and the target, never an unstated feature. | same | 4 C4-4 | accept | edit — remains edit after [run 4](extra-runs.md): repeated factor extraction changes population-related causal-role statuses between Unknown and met without a changed target, so the judgement remains unstable. Use “Unstated features leave causal role Unknown; not met requires a supported difference and an explanation of why it changes the causal role, while met also requires support.” Task 3 needs this under rulings 18 and 34; matching a programme's intended population is not by itself proof that the same mechanism operates. | |
| D5 | **Only dealbreakers cap** the support leg; helpful factors are shown, enter the conditions list when planned, and do not cap. Otherwise the verdict reads Unknown for every option unless the user states every factor. When no dealbreaker is named the leg reads "no necessary condition identified" and stays Unknown. | same; trust.md § transferability | 4 C4-2 | defer — revised after pass 4: the run's verdicts were fixed by a wrong causal-role judgement, so it cannot settle the support-leg rule | defer — remains defer after [run 4](extra-runs.md): the dealbreaker-only derivation stays Unknown in all eight cases, including the absent-condition case whose all-rows verdict is not met, and dealbreakers are found in only one of three factor runs. This measures the candidate's failure to resolve the question with the supplied factor lists, not proof that all helpful conditions should cap or that no dealbreaker exists. Before task 3 settles a replacement under rulings 18 and 29, necessary conditions and their support must be checked; changing which rows count cannot repair unreliable extraction. | |
| D6 | Whether a **local aggregate** (71 percent of residents live near a park) can set a factor met, or only a local resource observation can. | same | 4 C4-7 | lean: local aggregate is context only | edit — unchanged after extra runs; [Check 4 P5](check-4-local-condition.md) observes a local park-proximity percentage filling a safe-walking factor, but one mismatch does not establish that all local aggregates are unusable. Use “A local aggregate may meet only the proposition it measures for the stated target and period; proximity does not establish safety or access for every participant, and missing facts stay Unknown.” Task 3 should apply ruling 34's applicability discipline to local evidence too, rather than adding a blanket ban or treating a local resource observation as sufficient by its type alone. | |

## E — options-scoping components.md, capability.md, trust.md

| # | proposal | where | from | rec. | ChatGPT suggestion | decision |
|---|---|---|---|---|---|---|
| E1 | Re-mark **extract(abstract) as EB modified**, with an **`all_screened_in`** select strategy that includes Non-evidence so the abstract profile runs over every screened-in document. | components row 10; EB components § 3, § 6 | 6 F2 | accept | accept — unchanged after extra runs; [Check 6 F2](../check-6-contract-trace.md) identifies the selected-input requirement and Non-evidence exclusion in the existing interface, a contract/build observation rather than a performance measurement. Task 2 needs an explicit all-screened-in selection for the abstract profile and an honest EB-modified label under rulings 31 and 43; a mention from Non-evidence still cannot become outcome support. | |
| E2 | Screen row wording: evidence-scope constraints act at retrieval where expressible and as the set-aside after extract(abstract), **never at the screen**. | components row 3 | 6 F5 | accept | accept — remains accept after extra runs: [run 3](extra-runs.md) exercises set-aside using extracted geography, consistent with [check 6 F5](../check-6-contract-trace.md)'s ordering argument. Task 2 should keep the screen relevance-only under ruling 43 and C1's scope rules, but the limited country-name test and missing set-aside-only case do not establish complete enforcement. | |
| E3 | ⟨assess⟩ composition gains **`longlist(assign, finding grain)`** after extract(light) and runs synthesise(profile) under a declared `reading_scope`. | components § compositions | 6 F4 | accept | accept — remains accept after extra runs: [run 6](extra-runs.md) produces six model-assigned eligible findings from 49 quote-matched T3 records, demonstrating why the read set cannot stand in for finding eligibility. Task 3 still needs assignment between extraction and synthesis plus B1's declared reading scope under rulings 36, 38 and 44; neither assignment accuracy nor the complete production composition was validated by this count. | |
| E4 | `longlist` declares an **assign-only mode**; the discovery ceiling becomes **`clamp(ceil(N/4), 8, 40)`** (the old formula was binding in the thin corpus). | components § 6 | 6 F4, F9; 3 C3-3 | accept | edit — remains edit after extra runs: `suggestions.json` records 19 options at ceiling 25 and NEET records 20 at ceiling 21, so [the extra-run report](extra-runs.md)'s 20-option suggestion total and binding NEET ceiling do not match the files. Use “Support supplied-list assignment, retain uncovered candidates, and choose the discovery ceiling from checked recall rather than output count alone.” Task 2 still has open question 4 under rulings 31 and 44; the new formula was exercised but its benefit was not isolated from changed inputs or checked for missed options. | |
| E5 | Two guards on one-place-per-lever-type: a lever type whose options all have **zero evaluated mentions** earns a **gap message, not a place**; a place with a close runner-up lever or a near-equal rival is marked **contested** for the user to confirm. Relabelling moved four of five places in the thin corpus and seated a one-document unevaluated option. | components § 8; capability § shortlist | 3 C3-2 | edit — revised after pass 4: the instability is two of five seats, not four; keep the quota, decide the guards on corrected evidence | reject — remains reject after [run 1](extra-runs.md): NEET replaces the same chosen option in three lever perturbations, including one change of lever, which strengthens the instability concern but does not test either proposed guard. Withholding every unevaluated lever's place still conflicts with rulings 19 and 20, and the invalid zero-country implementation reason in run 3 needs correction without making evidence volume a selection gate. Before task 3, address unsupported tie reasons and test allocation by the substantive comparison; do not infer that a thin-evidence floor is the right remedy from where this one corpus was unstable. | |
| E6 | A run-keyed **`shortlist_result`** record (places, reasons, who filled them, gap messages, warnings) for export. | components § 8 | 6 F15 | accept | edit — remains edit after [run 3](extra-runs.md): the user-added place is written directly into a separate result field, not exercised through repeated proposals or between-run user edits. Use “Retain the proposal, reasons, author and later user changes against option/design and plan versions, including gaps and warnings.” Task 3 still needs the durable record declared alongside task 2's option state for task 4's export under rulings 5, 21 and 39; the fixture demonstrates a desired output, not persistence. | |
| E7 | Scoping **select strategy stratifies on the abstract profile's evaluated role × outcome family**, reserves one review and one primary study, and uses text availability as a tiebreaker only (full-text-first picked process evaluations with no effects). | components § 9; EB components § 6 | 5 C5-2 | accept | edit — remains edit, with the reserve claim weakened after [run 6](extra-runs.md): the revised selector gives T3 zero model-assigned eligible findings at cap 3, and its larger T4 selections repeatedly spend places on copies of one review. Use “Select for the specified design, required outcomes, distinct implementations and relevant contrary findings, reconcile duplicate documents and overlap before spending the cap, and retain implementation evidence needed for transferability.” Task 3 needs rulings 26, 38 and 43; preserving one T3 null result at cap 5 supports that particular selection, not the universal sufficiency of one review and one primary study. | |
| E8 | **Countable cells come from per-document extraction, never from reading alone** (one call over five texts missed contrary evidence extraction found). | components § 9–11 | 5 C5-3 | edit — revised after pass 4: the missed-counter-case claim was wrong; prefer extraction for verifiability (anchors, claim keys), not for this test | edit — remains edit after [run 6](extra-runs.md), but its new claim against reading alone is also unsupported: `counterev.json` contains a null-result tally and a source-specific account for T3, and null/mixed tallies for T4 that the runner misses on unmatched outcome labels. Use “Populate countable cells from verified per-document findings or compatible inherited findings; narrative reading must not introduce unrecorded counts or magnitudes.” Task 3 can retain this auditable composition under rulings 16, 26 and 35, but the measurement does not show that reading only flags contrary evidence without saying what or where. | |
| E9 | Wire the Evidence search's **quote vetter and claim-key dedup** for the light profile (17 percent of anchors failed unvetted; window repeats doubled one document's claims). | components § 10 | 2 C2-6 | accept | accept — remains accept after extra runs: pinned `light_v2.json` contains 173 findings and 38 failed anchors out of 223, about 17 percent rather than [the report](extra-runs.md)'s 20 percent, and repeated claims still appear in the equal-text output. Task 3 should apply quote vetting and coherent-claim dedup before eligibility and counting under rulings 16, 26 and 39; the runner's at-least-one-matching-anchor test is not a full claim-support check. | |
| E10 | The magnitude cell holds a **number with its unit only**; the source's own characterisation is a separate quoted field. | trust § effect cell | 2 C2-8 | accept | edit — unchanged after extra runs; [Check 2 C2-8](check-2-evidence-attribution.md) observes qualitative wording in the magnitude field, which supports separating it from numeric estimates but not stripping the estimate's qualifiers. Use “Keep the reported number or range with its measure, unit, uncertainty, comparator, population and period; preserve qualitative characterisation separately with its citation, and leave an unreported number empty.” Task 3 must retain the coherent claim required by rulings 16, 17 and 33, including ratios or percentages that are not meaningful as an isolated number and unit. | |
| E11 | **"How sure" as two numbers**: documents read, and independent own-data studies where identity is known; reviews are documents, never studies ("3 reviews, 0 independent primary studies read"). | trust § effect cell; capability § assessed evidence | 2 C2-1 | accept | reject — remains reject after [run 5](extra-runs.md): the pinned trace now counts five copies of one review and uses missing-identity document ids as study keys, while the gpt-5.5 model judge cannot check four of V1–V10 from the supplied excerpts. Counts still do not measure confidence in the specified design–outcome claim, and the two-distinct-studies/same-name case remains empty despite receiving a model-judge right label. Task 3 should keep the qualified claim-specific judgement and bounded, deduplicated counts alongside it under rulings 26 and 33, with unresolved independent-study totals suppressed. | |
| E12 | `synthesise(profile)` carries a **declared section budget** (the latency lever) and produces the transferability working in the two steps of D1. | components § 11 | 5 C5-1; 4 C4-1 | accept | edit — remains edit after [run 7](extra-runs.md): eight proxy sections now have a measured synthesis time of 380.224 seconds, but no actual scoping baseline, transferability profile or assessment report was produced together. Use “Declare and test bounded templates and reading allowances, preserving verification, essential qualifications and explicit incomplete outputs; check the factor list separately from context filling.” Task 1 can use the proxy to plan the baseline budget and task 3 the profile under rulings 2, 16 and 38, without treating it as a tested OS allowance. | |
| E13 | The factor step asks explicitly **what the evidence reported as blocking delivery** (it flagged no dealbreaker despite a governance block); a user may **add a condition** the evidence did not name, as a row typed *stated by you* with the evidence cell "not addressed by the evidence". | trust § transferability | 4 C4-3 | accept | edit — remains edit, with narrower support for the proposed fix after [run 4](extra-runs.md): 134 extracted context claims include 45 labelled blockers but receive no demonstrated quote vetting, and the model judge agrees that the exception fixture does not establish that the target is excluded. Use “Check reported blockers and their basis, preserve user-supplied conditions with the actual stated/planned provenance, and explain necessity and target-specific applicability before a condition affects the verdict.” Task 1 should retain the inputs and task 3 adjudicate them under rulings 18, 34 and 43; neither adding a field nor pinning model-labelled blockers establishes necessary conditions. | |
| E14 | Ruling 47's "prior version in History" is a **derivation edge** from the child artefact to the scoping-pass block version plus a display convention (history is linear per artefact; an artefact lives in one task). | components § ⟨full run⟩ | 6 F8 | accept | accept — unchanged after extra runs; [Check 6 F8](../check-6-contract-trace.md) traces the conflict between one-task artefact ownership and the promised prior scoping version; it proposes a compatible derivation link, not a tested display. Task 5 can use that link to show the child's one report in both homes and retain the earlier scoping block in History under ruling 47, provided “what changed” compares the pinned versions and export attaches only the current profile. | |
| E15 | Ruling 41 stands **on one condition**: the child computes membership against the specified design via `group`'s fixed-target-list mode or `longlist(assign)`; record the condition. | components § ⟨full run⟩; EB components § 8 | 6 F9 | accept | edit — remains edit after [runs 4 and 6](extra-runs.md): assignment and context errors now have additional examples, but no child Evidence search was run with the full OS judgement boundary. Use “The child applies specified-design eligibility, the profile scope and shared verification to the versioned context it receives, preserving unresolved conditions.” Task 5 still needs all these dependencies under rulings 41, 47 and 48; standalone assignment calls do not establish that membership alone makes the child safe. | |
| E16 | Record the measured rapid number against open question 3: about 25 minutes of compute for a one-option sense-check on today's synthesise. | capability § depths, § open decisions | 5 C5-1 | edit — revised after pass 4: record as a projection with unmeasured stages, not a measured rapid number | reject — remains reject after [run 7](extra-runs.md): 675.7 seconds is now measured for a fresh Evidence search using 78 already-acquired sources, with one proxy synthesis and no OS gates, baseline or inherited twin. About 25 minutes for the complete scoping path remains a projection, so the new measurement does not close open question 3. Task 1 should retain an honest provisional budget and tasks 3/4 measure the actual path with verification and separate gate waits under rulings 2, 3 and 38. | |

## F — Evidence search components.md (mirrors)

| # | proposal | where | from | rec. | ChatGPT suggestion | decision |
|---|---|---|---|---|---|---|
| F1 | § 0 inherit: `task_link` input; copy-or-rerun rule; `inherited_from_task_id`. | § 0 | 6 F6, F7 | accept with A6, A7 | edit — unchanged after extra runs; [Check 6 F6/F7](../check-6-contract-trace.md) identifies the shared inheritance inputs, but no cross-task run validates the copy policy. Use “Reference the shared Link and inherited accounting rules, with the snapshot/input compatibility checks in A7; re-screen in the receiving task.” Declare this shared contract before task 1 under rulings 22 and 48, while task 5 still owns composing the reverse direction rather than task 1 implementing it prematurely. | |
| F2 | § 3 classify: Non-evidence stays excluded from select/extract except under the `all_screened_in` strategy. | § 3 | 6 F2 | accept with E1 | accept — unchanged after extra runs; [Check 6 F2](../check-6-contract-trace.md) observes that the current exclusion would prevent extraction of the mentions ruling 43 requires. Task 2 should declare the abstract-profile exception explicitly alongside E1, keeping ordinary Evidence search selection/extraction and the rule that Non-evidence is not support intact; this is an interface correction, not evidence that the new profile's classifications are accurate. | |
| F3 | § 6 select: the refined scoping strategy (E7) and `all_screened_in`. | § 6 | 5 C5-2; 6 F2 | accept with E7 | edit — remains edit after [run 6](extra-runs.md): the membership-informed selector is exercised, but duplicates consume capped places and T3's cap-3 selection yields no model-assigned eligible findings. Use “Declare all-screened-in selection for the task-2 abstract profile separately from the task-3 assessment strategy, whose duplicate handling and coverage must satisfy E7.” Rulings 38 and 43 still require those distinct uses; the observed failures do not justify freezing the revised reserve rule into the shared component. | |
| F4 | § 7 extract: the two options-scoping profiles registered; pointer to the two ADR-wanting changes (A4, A5). | § 7 | 6 F3; 2 C2-7 | accept with A2–A5 | edit — remains edit after [the pin and run 5](extra-runs.md): the abstract/light profiles have further standalone runs and a model-judged partial older record, but no shared profile registration or cross-task reuse path was tested. Use “Register the abstract profile in task 2 and the verified light profile in task 3, preserving existing profile histories and defaults except for separately approved changes.” Task 1 still needs A4's bounded inheritance and A5's reading-depth rule under rulings 16, 35 and 43; pinned output fixes reproducibility of that run, not interface compatibility. | |
| F5 | § 8 group: **fixed-target-list mode** (discovery skipped; assignment against a supplied option list). | § 8 | 6 F9 | accept with E15 | defer — remains defer after [runs 2 and 6](extra-runs.md): assignment against supplied options is exercised, but neither a changed `group` nor the child composition is demonstrated. Tasks 2/3 should establish shared assignment behaviour and task 5 should choose its integration under rulings 36, 41 and 48; the runner's differing assignment results are further reason to validate the behaviour before selecting a production interface. | |
| F6 | § 9 synthesise: the `reading_scope` parameter; the Evidence search's own compositions unchanged. | § 9 | 6 F4 | accept with B1 | accept — unchanged after extra runs; [Check 6 F4](../check-6-contract-trace.md) identifies exactly why the shared synthesiser needs an explicit reading-scope parameter for scoping. Task 3 should add the declared opt-in boundary with B1's depth and exception rules while ordinary Evidence search keeps its current soft selection prior, satisfying rulings 38 and 44; enforcement across all read paths still needs demonstration. | |

## Also proposed, not spec

- A **Results** section in `feasibility-checks.md` summarising each check's outcome and what the
  owner still owes (factual; no design content). Decision: ____
- A **spec log entry** once the accepted rows are applied. Decision: ____
- Two **ADRs** for the Evidence search changes (A4 memo key; A5 `text_basis`). Decision: ____
