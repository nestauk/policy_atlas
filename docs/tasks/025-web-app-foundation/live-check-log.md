# 025 I.2 live check — narrated log

08:43:24 — servers confirmed up (healthz ok, frontend 200); Chrome connected; user A token pasted next.
08:48:00 — dev sign-in panel rendered (visibly non-production), token A pasted, signed in.
08:48:00 — FINDING (fixed live): projects fetch 404 — double /api prefix (client DEFAULT_BASE_URL "/api" + generated paths already /api/v1). Fixed client.ts + sse.ts to same-origin base; gates re-run green. Mock mode intercepts fetch, so Playwright could not have caught it.
08:48:00 — landing green as user A: honest empty state; pre-025 NULL-owner dev projects invisible (strict owner scoping working as designed).
08:51:55 — project A (755bc747): 2 real planner turns (refinement folded into question/scoping notes), plan disclosure verified (standard/standard/frequent/both, 9-step chain), Start the analysis clicked.
08:52 — A's run 88deb1b5 dispatched; acquire succeeded in 17s (49 found, adequacy=inadequate, stop=error — one search backend erroring is an env reality: no OVERTON_API_KEY in the live env); P1 steer point fired and the walk PARKED (status paused, options deepen_search/rescope_filters/guide_queries/accept_thin/abort, segment_reentry_allowed).
08:53-08:54 — project B (5cd7d0a0) created as user B via API; ONE real planner turn → ready (rapid/landscape/unattended); run bdae8bd1 dispatched 08:54:07.
08:54:41 — probes while A parked ∧ B executing: healthz 200 in 0.003s · funnel(A) 200 in 0.037s (pin: <2s) · B→A 404 byte-identical to never-existed · A→B 404 · second dispatch on B 409 · archive-while-running 409 run_active.
08:56:21 — RESTART MOMENT: killed the API server while A parked ∧ B executing (B mid-chain: synthesise). Lesson caught live: default SIGTERM = graceful drain, and the walk THREAD kept executing in the draining process while the new one booted — a real deploy-posture finding (shutdown must stop the walk executor, or deploys must hard-kill). SIGKILLed the old process 08:57:25 to make the honest mid-execution death.
08:57:43 — post-restart truth: A run paused (ended_at NULL) — parked run SURVIVED the restart by construction; A pending check-ins = exactly 1; B run interrupted (ended_at stamped by the orphan sweep). healthz ok.
08:59-09:01 — post-restart answer through the UI: free-text steer typed into the parked card → 202 compile → CONFIRM GATE rendered the interpretation ("add a screening criterion to prioritise OECD, IMF and national government sources") WITH the honest refused-fragment list (acquire already ran; 'screening' not a plan component) → Apply these changes clicked.
09:04-09:11 — TWO live-check findings fixed in place, each gate-checked: (1) C.3 confirm-apply passed the plan row as a dict into the steering persistence (attribute access) → 500; row now passed as-is. (2) Pre-existing 024 gap surfaced by router variance: the screen branch of _validate_directive_delta was the ONE of seven that didn't map its directive error into the refusal path → crash instead of honest refusal; wrapped like its siblings — the next malformed compile rendered an honest "Refused … must be a bounded list" line in the confirm gate.
09:10:46 — clean free-text compile ("Will apply → screen_abstract {screening.criteria: [prioritise OECD/IMF/government]}") APPLIED through the confirm gate: steering.decision + continuation.requested in one transaction → continuation.claimed → BOUNDARY CONTINUATION WALK executed screen_abstract with the applied criteria → Frequent mode parked again at the after-screen boundary (pause seq 139 + run.parked seq 140) — the park-per-boundary loop as designed.
09:13:19 — answered the second check-in change_mode→unattended (204) → continuation #2 claimed; walk now runs unattended to completion.
09:13-09:31 — walk ran unattended through the remaining chain (ingest 8 full texts, screen_full 13→11, characterise, select 11, extract skipped at standard depth as planned, synthesise) → run SUCCEEDED 09:31:50. Funnel: 49 found · 11 relevant · 12 quality-checked · 8 read in full · 11 selected · 8 cited.
09:32-09:34 — evidence base rendered: display-face question title; composed coverage sentence honest ("Searching stopped because error. Coverage was judged inadequate."); Key findings + sections with span-anchored underlined claims, inline citation markers, "Evidence gap" chip in the prose; citation click → popover (quote, tier_3 + Limited labels) → "In its surrounding context" clamped excerpt (the 008 chunk-context seam, live) → source dossier sheet with ?source= URL (deep-linkable). Reference-click dossier fully populated (status ladder cited/Strong/Cited, year, origin OpenAlex, evidence type label, DOI). FINDING (review): citation→dossier join is by title; a locator-fallback title misses the evidence row (honest empty state) — CitationOut should carry source_id. FINDING (review): no rename/archive control in the views (mutations exist + tested); actions exercised via API. FINDING (review, cosmetic): ingest_full_text presents under the acquire stage label ("Searching sources · Queries out…") while reading documents.
09:34:48 — rename persisted (PATCH); archive idempotent (200, 200) with EXACTLY one project.renamed + one project.archived audit event in event_log; SSE fix: continuation.claimed now maps to run.status running (the stale "Paused" banner during continuation execution — fixed + gated).
09:35 — landing truth: archived project hidden from the default listing (rows retained); ?status=archived lists it renamed/archived/succeeded; user B's landing shows their project honestly interrupted.
WALL TIME: ~52 min sign-in→landing-truth (incl. three live-fix cycles), inside the 60-min pin. B's timings: planned+dispatched in ~60s, executed acquire→synthesise for ~2.5 min before the restart killed it mid-synthesise.
