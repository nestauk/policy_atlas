# RETRO.md reconciliation sweep — task 025

Source: `origin/demo-live-run:demo/RETRO.md` (built 2026-07-09/10, itself
carrying a 2026-07-10 addendum noting §§1–3 orchestration description is
historical). Per the contract's "Read first" section and owner instruction
(2026-07-20): RETRO is read as point-in-time evidence, not a to-do list.
Every discrete claim in §§2–5 is classified below against the merged 001–024
slices, `docs/deferred.md`, and code on `dev` (spot-checked where cheap).

Legend: **DISCHARGED** (landed in a merged slice) · **STILL OPEN — 025**
(025 contract already covers it) · **STILL OPEN — deferred** (recorded in
deferred.md, or flagged as missing) · **SUPERSEDED** (no longer applicable) ·
**HOLDS** / **SUPERSEDED-BY-024** (§2 product decisions only) ·
**UNVERIFIED** (could not confirm either way — flagged honestly rather than
guessed).

## §2 — Product decisions (HOLDS vs SUPERSEDED-BY-024)

| # | RETRO item (§2) | Classification | Evidence |
|---|---|---|---|
| 2.1 | Locked terminology ("Start the analysis", "Screening" not "sift", "quality-check", "shortlist", "read in full", "pull out findings", "evidence base"; component names never reach UI) | **HOLDS** | Contract strand 7 explicitly re-binds RETRO §2 "unless superseded by as-built 019–024 behaviour" and names "locked vocabulary" as one of the four things carried forward verbatim. No 019–024 slice touches UI copy. |
| 2.2 | One orchestrator, one thread, two postures (plans conversationally, narrates, mediates check-ins; execution is a fixed driver) | **HOLDS, sharpened** | ADR 0021 (orchestrator-decider-dial) + AGENTS.md "one orchestrator, three moments: planning turn · free-text router · boundary watch" — the shape survived and gained the decider dial / steer-point lattice (024). The demo's single "narrate + mediate" posture is now formally the watch (triage/decide/route). |
| 2.3 | Chat shrinks 55/45→35/65 when analysis starts | **HOLDS** (frontend behaviour, unbuilt) | No backend concern; nothing in 019–024 supersedes it. Falls to 025 strand 7 to actually build — not contradicted by any as-built code. |
| 2.4 | Right pane: one card/line, execution-order stage timeline, outcome-first, sticky nav, "View plan ▾", `scrollbar-gutter: stable` | **HOLDS** | Pure frontend spec; contract strand 7 lists "workspace run view (stage timeline, activity feed, check-in cards with the 024 confirm-gate rendering)" as demo-validated and binding. |
| 2.5 | Annotation layer renders IN the prose (typed spans via `addressable_unit`, gap chip, no callout cards, no per-block Detail footer) | **HOLDS** | ADR 0015 (prose-first synthesis with span-anchored claims) is exactly this design, now backend-real, not just a demo mock. Contract strand 7 names "annotation layer IN the prose" as demo-validated and binding. |
| 2.6 | Hover→click citation ladder; shared dossier slide-over (status ladder, screening confidence/basis/stage, quality labels, abstract, DOI/publisher/cited-by/FWCI, tags by asserter, citing claims, findings) | **HOLDS**, one **SUPERSEDED-BY-024** sub-item | The ladder itself: unbuilt frontend, not contradicted. Sub-item drift: RETRO's dossier lists "FWCI" as delivered content, but deferred.md ("018's A/B-gated writer-envelope metadata queue") records FWCI as **still queued, never run** — the dossier's FWCI field is aspirational, not a shipped writer-envelope field. Tags-by-asserter still holds (ADR 0012 decision 8 `{tag, tag_type, asserted_by}`). |
| 2.7 | Labels not numbers (SCORE_LABELS, never 1–5) | **HOLDS** | `DEFAULT_RUBRIC` / rubric_version machinery (deferred.md "Steerable / plan-carried appraisal rubric") is unchanged; no slice introduced raw numeric score surfacing. |
| 2.8 | Decision log user-facing; projection over `event_log` with an allowlist; search query strings persisted durably (not merely in-memory-bus-merged) | **HOLDS**, underlying gap **DISCHARGED** | The demo's own caveat ("a real slice should persist queries durably") is already moot on `dev`: `acquire.py` emits a durable `search.executed` event (`event_type="search.executed"`) carrying query text/backend/filters/origin/result count — confirmed by grep. Not an in-memory-bus artefact. The *decision-log-as-allowlisted-projection* pattern itself is what 024's `steering_history` projection already generalises, and 025 strand 4 explicitly serves check-in history "from `steering_history`, never from transport memory." |
| 2.9 | Data-driven surfaces (Findings tab only if findings exist, hide-never-fake, landscape distributions over SCREENED-IN set only via `status == "relevant"`, funnel spans full flow, mock data sums consistently) | **HOLDS**, verified | `characterise.py` still filters `effective.c.status == "relevant"` for the screened-in set (grep-confirmed, line ~391) and computes `screened_in = status_counts.get("relevant", 0)`. Contract's "Disciplines binding this slice" restates "Flag, don't drop" / hide-never-fake as a 025 discipline verbatim. |
| 2.10 | Publication country derivable read-time but must be labelled "where published, not where conducted"; provider tag layer too dirty for headline chart, belongs in per-source dossier grouped by asserter | **HOLDS** | deferred.md's "Study-geography extraction field" entry (task 020, ADR 0016) confirms `study_geography` (where the study was CONDUCTED) is now a distinct, landed extraction field — reinforcing rather than superseding the RETRO distinction; publication-country-as-derived-metadata is unchanged, still a read-time derivation, not a stored field. |
| 2.11 | Coverage honesty needs one sentence (stop condition + adequacy composed server-side, not two confusing separate lines) | **HOLDS, and now literally re-stated in the 025 contract** | `search_coverage_record` still stores `stop_condition` and `adequacy_verdict` as two separate columns (schema.py, grep-confirmed) — composition is still a read-model/presentation job, not yet done. Contract § "Disciplines binding this slice" copies this almost verbatim: "Honest absence — coverage claims carry their base; the composed coverage sentence (stop condition + adequacy in one line) is server-side." |
| 2.12 | Motion budget (count-up, growing bars, breathing progress, rising cards, amber check-in glow; `prefers-reduced-motion`) | **HOLDS** | Contract explicitly requires "`prefers-reduced-motion` honoured" as a browser check under Acceptance checks. Pure frontend, unbuilt, not superseded. |

## §3 — Contract-discipline lessons

| # | RETRO item (§3) | Classification | Evidence/citation |
|---|---|---|---|
| 3.1 | Mock/live contract divergence was the #1 bug class (`plan?.steps` crash, mock facet names vs live vocabulary, `year_range` string vs `{min,max}`); fix rules: mock implements the same TS interface as live, mock fixtures mirror live vocabulary, front-end never trusts optional fields, "a real slice should generate types from one schema" | **STILL OPEN — 025** (the fix is the 025 design, not yet built) | Contract strand 2 verbatim: "Schema-first: Pydantic response/request models are the single contract; OpenAPI is generated from them; the TypeScript client is generated from the OpenAPI document; CI fails on drift... (Demo evidence: mock/live contract divergence was the #1 bug class — RETRO §3.)" This is the lesson's direct, cited discharge-in-progress — the contract quotes RETRO §3 by name as its own rationale. Not yet built, hence still open rather than discharged. |
| 3.2 | SSE backlog replay is the resilience model (reconnect replays everything; store rebuilds idempotently from events alone; chat turns re-emitted with text-dedup; survived restarts/tab nav); in-memory bus loses history on restart — "real slice needs durable events (mostly already true via event_log)" | **STILL OPEN — 025** | Contract strand 5 ("Durable event transport") is exactly this, built on the durable substrate: "SSE per project with backlog replay served from `event_log`... the frontend store rebuilds idempotently from replay alone (demo-proven resilience model, now on the durable substrate — survives server restart mid-run *including* a pending check-in)." Explicitly not yet built; the parked-pause / continuation-walk redesign (owner, 2026-07-21) is a *further* refinement beyond what RETRO anticipated. |

## §4 — Live-run observations

| # | RETRO item (§4) | Classification | Evidence/citation |
|---|---|---|---|
| 4.1 | characterise discovery is the #1 live wobbler (failed twice on a 47-doc corpus, whole tail collapsed); driver now retries the stage once; synthesise skips honestly on zero substrate; rejection *reason* still only in Langfuse (`_discover_themes` discards validator detail) | **DISCHARGED — task 019** | `theme_grouping.py` defines `DISCOVERY_RETRY_CAP = 1`, threaded into `characterise.py`'s `_characterise_clustering_policy` (`discovery_retry_cap=theme_grouping.DISCOVERY_RETRY_CAP`) — the retry-once behaviour is real. The "reason only in Langfuse" half is also discharged: `characterise.py::_grouping_provenance` now carries `discovery_rejections: list[str]` into the persisted grouping provenance, matching deferred.md's own note: "Compounding gap — DISCHARGED (task 019): `str(exc)`, truncated to 500 chars, is now carried in logs and failure records on both the discovery and assignment paths (previously only `error_type` reached the logs)." |
| 4.2 | `FACET_VALUE_CAP=150` binds at real scale (25 docs → 427 findings → 280 facet values); demo cap 400 worked; monkeypatch gotcha (`group.py` imports the constant by value — patch both modules) | **DISCHARGED — task 022** | `facet_values.py` now defines `FACET_VALUE_CAP = 400` as the single source of truth, imported once by `group.py` (grep-confirmed, one import site) — the "patch both modules" monkeypatch hazard is structurally gone (no monkeypatch exists in production code; the demo's `driver.py` patch was demo-only glue per RETRO's own 07-10 addendum). deferred.md: "**Very-large-corpus grouping** — **NARROWED (task 022)**: the two-stage engine closed the one-call exhaustive-partition cliff... `FACET_VALUE_CAP=400` stays the fail-closed input guard, now at FACET grain." Discovery-input-scale-beyond-cap remains a distinct open seam (deferred.md, same entry) — not part of this RETRO claim. |
| 4.3 | Failure chaining: a failed stage must never feed its run id downstream (synthesise `missing_referenced_row`); driver pattern: stages chain only off successful predecessors, synthesise takes the deepest successful reference | **DISCHARGED — task 013 (synthesise build), reaffirmed in orchestration_plan.py** | `synthesise.py` raises `SynthesiseFailure(f"missing_referenced_row: {name}")` when a referenced row is absent (grep-confirmed); `orchestration_plan.py` names `"synthesise": "deepest_successful_reference"` as the reference-resolution strategy in the reference-shape table. This is exactly the RETRO-described pattern, now named, tested machinery rather than a driver-level ad hoc fix. |
| 4.4 | Extraction quality on live grey lit is the weakest link (vague labels, unexpanded acronyms, aspirational boilerplate extracted as findings); prompt rules added (self-contained naming, expand acronyms, no hortatory statements, concrete outcomes) — NOT yet validated by a post-fix run; a findings quality gate/judge is the next lever | **DISCHARGED (rules landed) / STILL OPEN — deferred (quality unvalidated)** | `iof_prompt.py` (grep-confirmed) carries exactly these rules verbatim: "Expand every acronym the document defines, keeping the short form"; "A finding whose intervention and outcome cannot be named self-containedly... too vague to extract"; language banning boilerplate "reaffirming"/"committing to" a communiqué. AGENTS.md / contract confirm this is `extract_iof_v7`. The *validation* half is still open: deferred.md's "Extraction-quality evals" entry (task 011 seams) remains an open eval-gated seam — "extraction *quality* (right/complete findings) is unmeasurable without" ground truth, sequenced for the eval slice, after 025 per project-state. |
| 4.5 | Synthesis voice: no prose guidance → claims read as tool returns, pattern claims recited pipeline statuses; voice rules added (banned internal vocabulary, analyst-style restatement, takeaway-first ordering, one citation per source per claim, patterns describe evidence never processing) — NOT yet validated | **DISCHARGED (rules landed, task 018 B-B2) / STILL OPEN — deferred (quality unvalidated)** | `synthesis_backend.py`'s module docstring dates the voice design to "task 018 B-B2: the deliberate voice design," now at `synthesise_section_v8` (`SECTION_PROMPT_VERSION = "synthesise_section_v8"`, superseding the v7 named in AGENTS.md/contract — confirms iteration continued past 022). "Takeaway-first" language is grep-confirmed live in the prompt. Validation remains open under the same eval-slice gate as 4.4 (deferred.md: "Synthesis / judge / retrieval quality evals" — "the field's loudest warning... shipping without an eval harness — makes this the recommended next slice"). |
| 4.6 | References/titles: full-text snapshots carry no title metadata; reference entries must resolve display metadata via the owning source's ENVELOPE snapshot | **DISCHARGED** (task uncertain — **UNVERIFIED which slice**, but landed) | `synthesise.py` resolves `document_title` from `metadata_by_snapshot` (envelope metadata), with a fallback string only when metadata truly lacks a title (grep-confirmed at two call sites). The mechanism described by RETRO is live code today. Could not pin down which numbered task introduced it from grep alone (it may predate the demo, i.e. already true in task 013's synthesise build) — flagging as **UNVERIFIED (task attribution only)**, not the underlying fact. |
| 4.7 | Fetch reality for 016 (222 attempts, 81 ingested, ~39 fetch_failed + 22 parse_failed; paywall/empty-body reasons; prefetch-parallel worked; OpenAlex rate-limits back-to-back runs) | **SUPERSEDED** (historical run-log numbers, not a lesson to carry) | These are one-off live numbers from a single demo run, not a design lesson. deferred.md's "Live `DocumentFetcher` — DISCHARGED (task 016)" confirms the fetcher itself (the mechanism these numbers describe) shipped in full with the access-failure ladder (paywall/blocked_by_host/etc.) already covering the observed failure modes. Nothing for 025 to act on beyond what 016 already built. |
| 4.8 | Timing (live gpt-5-mini chain): quick ≈8–12 min bottlenecked by screening+characterise; deep run ≈95 min; extraction ≈35 min for 25 docs; "for a 12-min demo: pre-run deep + live quick" | **SUPERSEDED** (demo-logistics observation, not a 025 concern) | This is a demo-day rehearsal tactic (pre-running a deep project for a live demo), not applicable to a production web app. The underlying timing/depth-gradation *mechanism* it comments on is covered separately by item 4.9 below. |
| 4.9 | Depth must gate the chain (quick = acquire→screen→classify→appraise→characterise→synthesise; orchestrator should compile this — "the tool-wide depth-gradation seam in deferred.md") | **DISCHARGED — task 019 (two-axis effort/depth), residual seam STILL OPEN — deferred** | AGENTS.md / project-state memory confirm 019 shipped the two-axis effort/depth gradation, superseding the demo's binary quick/deep. The residual "tool-wide depth/time-budget gradation" entry is still explicitly open in deferred.md (task 015 seams section): "the orchestrator compiles a user time/quality preference into per-component depth directives + budgets... at the plan-as-object compile surface" — recorded, not fully closed, but the core mechanism RETRO worried about (an ungated fixed chain) is gone. |
| 4.10 | Liveness gap: nothing emits user-grade progress except token-usage telemetry; demo added 4 log lines in `search_live.py` + proxied `.usage` ticks with client-side collapse; "a real slice wants a designed component-progress protocol instead" | **STILL OPEN — 025 (partial) / deferred (full protocol)** | Contract strand 5 names this directly: "Liveness ticks that have no durable home (search/fetch progress) flow on a clearly labelled ephemeral channel — best-effort, never state-bearing. 🟡 A fully *designed* component-progress protocol (RETRO §4) is out; this slice ships stage-grain durable events + the minimal ephemeral tick channel, and the seam is recorded in deferred.md." Confirmed also in deferred.md ("arrives with that refactor or the component-progress protocol"). The demo's specific 4 `search_live.py` log lines were **not found in current `search_live.py`** (grep for query-issued/results-per-backend log lines returned no matches) — those exact lines look demo-only glue, not carried onto `dev`; the *general* progress-signal problem is what's tracked, not those specific lines. |
| 4.11 | Model routing observed: screen (mini×3) fine, classify fine, characterise discovery+extraction wobble on mini, synthesis needed gpt-5.5, judge stayed mini | **HOLDS as current routing, no longer monkeypatch-dependent (config now the real default)** | Confirmed live in code: `theme_grouping.DISCOVERY_MODEL = "gpt-5.4-mini"`, `theme_grouping.ASSIGNMENT_MODEL = "gpt-5.4-mini"`, `grounding_judge.JUDGE_MODEL = "gpt-5.4-mini"`, `synthesis_backend.SYNTHESIS_MODEL = "gpt-5.5"` — exactly the routing RETRO observed, now first-class module constants rather than the demo's `driver.py` monkeypatch. Not superseded, not "open" — this is simply the shipped configuration; classified as a durable fact, no 025 action implied. |

## §5 — Known rough edges left on the (throwaway) branch

| # | RETRO item (§5) | Classification | Evidence/citation |
|---|---|---|---|
| 5.1 | Obesity quick-run project stuck failed-at-characterise (pre-retry-fix); dud "Untitled project" cards litter landing; no delete endpoint; sidecar `demo/server/projects.json` is the registry | **SUPERSEDED** | This is demo-branch-local debris. 025 replaces the whole registry model: contract strand 3 ("Project lifecycle done properly") gives `project` real Postgres columns (`name`, backfilled to "Untitled project" only as a migration fallback for **pre-existing dev-DB rows**, explicitly the *same two known live-run projects* — "pre-existing rows keep `owner_user_id NULL` and are intentionally inaccessible... the dev DB's two live-run projects are the known case"). The `projects.json` sidecar has "no successor" by design (contract: "the demo's `projects.json` sidecar registry has no successor"). Delete becomes real archive semantics (strand 3). Nothing carries forward from the demo registry itself. |
| 5.2 | Landing "Paused — waiting on your input" state exists but nothing feeds it (needs a paused signal on `/projects`) | **DISCHARGED (by design, still to be wired in 025's frontend)** — closer to **STILL OPEN — 025** | The paused *signal* now exists structurally: `capability_run.status` includes `paused` (contract's gate-expansion migration, finding 1) and "the landing card's running/paused/complete presentation is a read model derived from the latest capability run" (contract strand 3, finding 6). The backend signal is designed-for in 025; the frontend "Paused" card consuming it is 025 Deliverable work, not yet built — hence still open as an implementation item, not a gap in design. |
| 5.3 | One analysis at a time (server 409s); bus history in-memory only | **STILL OPEN — 025 (superseding design), SUPERSEDED (bus specifically)** | "One analysis at a time" is being generalized, not just carried forward: contract's API design pins make "one active run per project" a Postgres-enforced guard (row lock / advisory lock, finding 16) rather than the demo's in-app 409, and *concurrent* users on *different* projects become first-class (owner pin, 2026-07-21) — a materially different (looser, correctly-scoped) constraint than the demo's single global "one analysis at a time." "Bus history in-memory only" is superseded outright by strand 5's durable `event_log`-backed SSE replay. |
| 5.4 | Prompt fixes (extraction rules, synthesis voice) unvalidated by a fresh run | **STILL OPEN — deferred** | Same eval-gate as items 4.4/4.5 above: deferred.md's extraction-quality and synthesis/judge/retrieval-quality eval entries are the recorded, not-yet-run validation; AGENTS.md pins the eval slice as coming *after* 025 and 026. Rules themselves are discharged (landed in prompts); validation is the open piece. |
| 5.5 | Branch uncommitted at time of writing | **SUPERSEDED** | Immaterial to 025 — `demo-live-run` is explicitly "throwaway, never merges" (RETRO header) and the contract confirms: "The `demo-live-run` branch itself stays untouched and unmerged; after this slice it is historical evidence only." |

## Items that looked like they might fall through the cracks (double-checked)

- **§2.11 / coverage-sentence composition** and **§3's schema-first mock/live
  fix** are the two items where the 025 contract *quotes RETRO by name* as its
  own justification — these are not at risk of being silently dropped; they're
  load-bearing contract language.
- **§4.10's liveness/progress protocol**: the *general* problem is tracked
  (contract strand 5 + deferred.md), but the specific demo mitigation (4 log
  lines in `search_live.py`, `.usage`-tick proxying) was not found anywhere on
  `dev` — confirmed as demo-only glue that did not carry forward, and nothing
  in 025's contract asks for those specific log lines to be ported. Flagging
  this explicitly since the contract's Scope/In line ("the four
  `demo-live-run` carry-forward candidates (search liveness log lines, prompt-
  rule and voice edits) **only if** verified still-unlanded on dev — check,
  don't assume") calls exactly this check out. **Verified: the search-liveness
  log lines are NOT on dev and are NOT independently required by 025** — they
  were a demo-only UX patch for the token-telemetry gap, superseded in intent
  by the ephemeral tick channel 025 is building instead.
- **§2.6's FWCI-in-dossier claim**: this is the one §2 item that reads as
  "still true" on a casual pass but is actually resting on an unshipped
  writer-envelope field (deferred.md's "018's A/B-gated writer-envelope
  metadata queue" — FWCI queued, never run). Flagged above as a
  SUPERSEDED-BY-024 sub-item so the 025 frontend doesn't build a dossier field
  that has no backing data yet.

## Summary of counts

- §2 (12 items): 10 HOLDS, 2 with a SUPERSEDED-BY-024 sub-item (2.6 FWCI, cross-referenced above).
- §3 (2 items): 2 STILL OPEN — 025 (both directly cited by the 025 contract as its own rationale).
- §4 (11 items): 5 DISCHARGED (4.1, 4.2, 4.3, 4.9-partial, 4.11-as-config), 2 DISCHARGED-with-validation-still-open (4.4, 4.5), 1 DISCHARGED-but-task-UNVERIFIED (4.6), 2 SUPERSEDED (4.7, 4.8), 1 STILL OPEN — 025/deferred split (4.10).
- §5 (5 items): 1 SUPERSEDED outright (5.1), 1 mixed DISCHARGED-design/STILL OPEN-frontend (5.2), 1 mixed STILL OPEN/SUPERSEDED (5.3), 1 STILL OPEN — deferred (5.4), 1 SUPERSEDED (5.5).

Nothing in §§3–5 was found genuinely uncovered by both 025's contract and
deferred.md — the two items that looked most at risk (liveness log lines,
FWCI dossier field) are addressed explicitly above.
