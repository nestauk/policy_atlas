# Splash page design source

Bundled design prototype for the logged-out marketing home.

- Runtime implementation: `frontend/src/views/splash/`
- Fold-mark geometry: `frontend/src/ui/brand/foldMark.ts`

The HTML bundle is reference-only — do not load it in the app.
